name: notepadqq
buildsystem: qmake
config-opts:
  - QMAKE_STRIP=
  - LRELEASE=lrelease
build-options:
  env:
    QMAKEPATH: "/app/lib"
sources:
  # Source0
  - type: archive
    url: https://github.com/notepadqq/notepadqq/archive/beta.tar.gz
    sha256: f0a568770106d896419f7af2d509d14d422f5dc0a32b86ac4aad38c854d0929f
  # Patch0
  - type: patch
    path: notepadqq-beta-xdg.patch
post-install:
  # create symlink
  - echo 'create symlink';
    ln -sf "${FLATPAK_DEST}/lib/notepadqq/notepadqq-bin" "${FLATPAK_DEST}/bin/notepadqq";
  # install docs
  - echo 'install docs';
    install -d "${FLATPAK_DEST}/share/doc/notepadqq";
    install -p -m 0644 *.md "${FLATPAK_DEST}/share/doc/notepadqq/";
  # install licenses
  - echo 'install licenses';
    install -d "${FLATPAK_DEST}/share/licenses/notepadqq";
    install -p -m 0644 "COPYING" "${FLATPAK_DEST}/share/licenses/notepadqq/";
